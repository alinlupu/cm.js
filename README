DEVELOPMENT RULES

1. use features supported by default by:
	chrome 51+
	firefox 54+
	edge 14+
	safari 10+
	opera 38+

	do not use:
		es6 class
		I prefer not using class syntax because it is not fully supported by modern browsers yet
			
2. if params > 3, use params obj = {}
